# Self-hosted on the Oracle VM as of 2026-07-31 -- same origin as the
# cockpit itself, so the default in src/App.jsx is "" (same-origin) and
# this file only needs to be copied to .env if you want to point a build
# at a different backend (e.g. a standalone instance elsewhere).
VITE_CLOUD_API_URL=
