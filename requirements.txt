# ─── QuantOS Dependencies ────────────────────────────────────────────────────

# Web framework (cloud API)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Claude AI
anthropic>=0.28.0

# Broker SDKs
fyers-apiv3>=3.0.0
kiteconnect>=5.0.0

# Data & quant
pandas>=2.2.0
numpy>=1.26.0
ta>=0.11.0           # Technical analysis indicators
statsmodels>=0.14.0  # Engle-Granger/ADF cointegration test (pairs trading)
scikit-learn>=1.5.0  # ML multi-factor stock ranking (candidate 16)

# Database (cloud — Postgres OR SQLite, see cloud/api/db.py)
sqlalchemy>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.20.0    # Self-hosted VM deployment (2026-07-31) uses SQLite, not Postgres
alembic>=1.13.0      # Migrations

# Notifications / agent HTTP
requests>=2.31.0     # Sync HTTP (agent → cloud REST poll loop)

# Config & utilities
pyyaml>=6.0.1
python-dotenv>=1.0.0
httpx>=0.27.0        # Async HTTP (cloud → Telegram)
eval_type_backport>=0.4.0  # Python 3.9 compat for `X | None` syntax used throughout

# Scheduling (morning brief, screener)
apscheduler>=3.10.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
python-multipart
